<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <solid android:color="@android:color/transparent" /> <!-- 내부 채우기 색상 -->
    <stroke
        android:width="2dp"
        android:color="@color/MintGreen" /> <!-- 테두리 색상과 두께 -->
    <corners android:radius="0dp" /> <!-- 테두리 모서리 둥글기 -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- 내부 패딩 -->
</shape>